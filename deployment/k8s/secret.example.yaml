apiVersion: v1
kind: Secret
metadata:
  name: molecule-ranker-secrets
  annotations:
    molecule-ranker.openai.com/example-only: "true"
type: Opaque
stringData:
  # Replace through your cluster secret manager or ExternalSecret controller.
  # Do not apply this file with real values committed to git.
  auth-secret: "replace-with-long-random-auth-secret"
  database-url: "replace-with-postgres-database-url-from-secret-manager"
